<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pal-Engke | Profile</title>
    <link rel="icon" type="image/png" href="/images/robot-logo.ico">
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
        rel="stylesheet"
    >
    <link rel="stylesheet" href="/styles/profile.css">
    </head>

    <body id="profile-body">
    <div class="container py-5">
        <div class="card shadow-lg border-0 rounded-4 mx-auto" style="max-width: 600px;">
        <div class="card-body text-center">
            <img 
            src="/images/default-profile.png" 
            alt="User Avatar" 
            class="rounded-circle mb-3" 
            width="120" height="120"
            >
            <h3 class="fw-bold mb-0"><%= user.firstName + ' ' + (user.lastName ? ' ' + user.lastName : '') %></h3>
            <p class="text-muted mb-4">
                <% if (user.userType === "user") { %>
                    User
                <% } else if (user.userType === "premium_user") { %>
                    👑 Premium User
                <% } else { %>
                    <%= user.userType %>
                <% } %>
            </p>

            <div class="list-group text-start">
            <div class="list-group-item d-flex justify-content-between align-items-center">
                <strong>Email</strong>
                <span><%= user.email %></span>
            </div>
            <div class="list-group-item d-flex justify-content-between align-items-center">
                <strong>Contact</strong>
                <span><%= user.contactNumber || 'Not provided' %></span>
            </div>
            <div class="list-group-item d-flex justify-content-between align-items-center">
                <strong>Age</strong>
                <span><%= user.age || 'Not provided' %></span>
            </div>
            </div>

            <div class="d-flex justify-content-center gap-3 mt-4">
                <a id="btn-backToHome" href="/home">Back to Home</a>
                <form action="/auth/logout" method="POST">
                    <button id="btn-logout" type="submit">Logout</button>
                </form>
            </div>
        </div>
        </div>
    </div>

    <%- include('partials/navbar', { activePage: 'profile' }) %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
